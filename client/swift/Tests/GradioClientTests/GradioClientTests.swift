import XCTest
@testable import GradioClient

final class GradioClientTests: XCTestCase {
    func testInitDoesNotCrash() async throws {
        // This is a placeholder test to ensure the Swift package structure is valid.
        // In CI we can point this to a real demo URL.
        XCTAssertTrue(true)
    }
}

